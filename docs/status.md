# STATUS DOCUMENT (CONTINUITY)
> Актуализируется вручную ИИ в конце каждой фазы.

**ТЕКУЩИЙ СТАТУС:** ФАЗА 6 (Export & Final Docs) + РЕФАКТОРИНГ ЛОГИКИ НДС

## Выполненные задачи (Phase 6):
- Навигационный Sidebar с 5 ссылками на модули
- Вкладка SettingsView
- CSV Export (Ledger, Contradiction) с встроенным BOM и заголовками на иврите.

## Актуальные блокираторы (Blockers):
1. **Генерация PDF (Иврит):** `jsPDF` искажает иврит, так как не имеет VFS шрифтов. Используем `window.print` или нужно будет инжектировать шрифт (см. `lessons_learned.md`).
2. **НДС Расчет (Critical!):** В базе `0000_initial_schema.sql` дефолт `0.17`, в логике `0.18`. Будет расхождение в Ledger.

## Следующие шаги:
1. Исправить SQL миграцию + API `api/export` для унификации НДС = 18%.
2. Реализовать или улучшить модуль генерации `VO Letter.pdf` для смет.
